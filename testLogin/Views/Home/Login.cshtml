
@{
    ViewBag.Title = "Login";
}
<form>
    @*@using (Html.BeginForm("Index", "Home", FormMethod.Post))
        {
            <input type="text" id="txtName" name="name" />
            <input type="submit" id="btnSubmit" value="Get Current Time" />
        }
        @if (ViewBag.Message != null)
        {
            <script type="text/javascript">
                alert("đăng nhập thành công");
            </script>
        }
        @if(ViewBag.Message == null)
        {
            <script>
                alert("đăng nhập không thành công");
            </script>
        }*@

    <div class="mb-3">
        <label for="exampleInputEmail" class="form-label">Email address</label>
        <input class="form-control user" placeholder="nhập user">
        @*<div id="emailHelp" class="form-text">We'll never share your email with anyone else.</div>*@
    </div>
    <div class="mb-3">
        <label for="exampleInputPassword" class="form-label">Password</label>
        <input type="password" class="form-control pass" id="pass" placeholder="nhập pass">
    </div>
    <button type="submit" class="btn btn-danger btnLogin" style="margin-top:10px" onClick="checkLogin()">Đăng nhập</button>
    @*@Html.ActionLink("Đăng nhập", "Success", "Home")*@


    @* test :*@
    @*<a class="btn btn-primary" href="@Url.Action("Submit","HomeController")">Click</a>*@
</form>
